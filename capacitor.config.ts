import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.ionic.lukimgather',
    appName: 'Lukim Gather',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
